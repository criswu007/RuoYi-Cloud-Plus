<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.address.mapper.SegmAddrTypeMapper">

    <select id="selectLevelIdByAddrTypeId" resultType="java.lang.Integer">
        select max(level_id)
        from segm_addr_type
        where addr_type_id = #{addrTypeId}
    </select>

    <select id="selectAddrTypeIdsByLevelId" resultType="java.lang.String">
        select addr_type_id
        from segm_addr_type
        where level_id = #{levelId}
        order by addr_type_id
    </select>

    <select id="selectNextLevelId" resultType="java.lang.Integer">
        select min(level_id)
        from segm_addr_type
        where level_id &gt; #{currentLevelId}
    </select>

    <select id="selectFirstAddrTypeIdByLevelId" resultType="java.lang.String">
        select min(addr_type_id)
        from segm_addr_type
        where level_id = #{levelId}
    </select>

</mapper>
